# Toggle synthetic demo mode: when "true", the app reads bundled synthetic JSON
# from public/demo-data/ instead of the FastAPI backend.
#   - Public demo / production build: true (see .env.production)
#   - Local dev against the backend: leave unset
# For a local demo preview: VITE_DEMO_MODE=true npm run dev
VITE_DEMO_MODE=true
